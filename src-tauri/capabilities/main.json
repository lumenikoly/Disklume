{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local application window; no arbitrary filesystem, shell, opener or network permissions.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-clearmap"]
}
